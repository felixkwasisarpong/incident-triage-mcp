1. Get the service endpoint:
{{- if eq .Values.service.type "NodePort" }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "incident-triage-mcp.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else }}
  kubectl get service {{ include "incident-triage-mcp.fullname" . }} --namespace {{ .Release.Namespace }}
{{- end }}

{{- if .Values.agentJob.enabled }}

2. Inspect the agent Job run:
  kubectl get job {{ include "incident-triage-mcp.fullname" . }}-agent --namespace {{ .Release.Namespace }}
  kubectl logs job/{{ include "incident-triage-mcp.fullname" . }}-agent --namespace {{ .Release.Namespace }}

  # Re-run manually (delete the completed job, then helm upgrade again)
  kubectl delete job {{ include "incident-triage-mcp.fullname" . }}-agent --namespace {{ .Release.Namespace }} --ignore-not-found
{{- end }}
